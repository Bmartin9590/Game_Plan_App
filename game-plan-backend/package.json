{
  "name": "game-plan-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
"scripts": {
  "dev": "nodemon src/index.js",
  "start": "node src/index.js",
  "seed": "node prisma/seed.js",
  "setup": "npm install && npx prisma generate && npx prisma migrate dev --name init_schema && npm run seed && npm run dev"
},
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prisma": "^6.19.0"
  }
}
